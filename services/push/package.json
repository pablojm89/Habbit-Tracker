{
  "name": "bittracker-push",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "wrangler deploy --dry-run --outdir dist",
    "test:runtime": "npm run check && node test/runtime.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": { "luxon": "3.7.2", "web-push": "3.6.7" },
  "devDependencies": { "wrangler": "4.131.1", "miniflare": "5.20260911.0-alpha" }
}
